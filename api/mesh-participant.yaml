apiVersion: wirepact.ch/v1alpha1
kind: MeshParticipant
metadata:
  name: participant
spec:
  deployment: deploy
  targetPort: 8080
  service: svc
  translator: wirepact-operator-basic-auth-translator
  env:
    KUBERNETES_SECRET: credentials
